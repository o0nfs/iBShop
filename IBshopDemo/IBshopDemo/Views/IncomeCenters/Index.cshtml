@model IEnumerable<IBshopDemo.Models.IncomeCenter>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IncomeCenterName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IncomeCenterName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IncomecenterId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IncomecenterId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IncomecenterId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
